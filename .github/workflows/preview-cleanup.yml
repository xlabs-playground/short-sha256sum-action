on:
  pull_request:
    types: [closed]

jobs:
  preview-clean-up:
    runs-on: "ubuntu-latest"
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          ref: 'preview'
      - name: Compute Version
        id: version
        run: |
          echo "_BRANCH_NAME_SHA=$(echo ${{ github.ref }} | sha256sum | cut -c -10)" >> "${GITHUB_ENV}"
      - name: Set up Node environment
        run: |
          echo ${_BRANCH_NAME_SHA}
          echo ${{ github.ref }}
          cat ${_BRANCH_NAME_SHA}/.latest